import React from 'react';
import { Outlet } from 'react-router-dom';

const AllProduct = () => {
    return (
        <div>
            <Outlet />
        </div>
    );
};

export default AllProduct;